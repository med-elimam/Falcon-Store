# Falcon Store Hero Production

This folder contains the editable After Effects project and production notes for the homepage hero video.

Source media is stored in `source/`. Rendered deliverables are stored in `renders/`.
